改进：
跟V1.0版本相比，主要变化点
1、路由方面整理变化，使用配置的方式来进行路由设置。
2、每个模块有单独的路由，顶层只管理模块级别的路由，模块内的路由，模块内部具体实现
3、增加了热部署，删除了一些没有 用到的插件

问题:
1、存在一个问题，切换模块，会渲染两次，出现原因，在于 redux 中 store的中间件，关键redux-simple-router ，去掉则可以，但是会报错，因为派发action的时候用到